'use client'

export default function PageBreakEditor() {
  return (
    <div style={{
      borderTop: '2px dashed var(--border)',
      textAlign: 'center',
      padding: '4px',
      fontSize: '11px',
      color: 'var(--text-secondary)',
    }}>
      — Page Break —
    </div>
  )
}
